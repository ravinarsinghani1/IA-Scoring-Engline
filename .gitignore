# Dependencies
node_modules/

# Build output
dist/
build/

# SQLite database files (local dev data — do not commit)
server/data/*.db
server/data/*.db-journal
server/data/*.db-wal
server/data/*.db-shm

# Uploaded student PDFs (real student data — never commit)
server/data/uploads/

# Environment / secrets
.env
.env.local
*.local

# Logs
*.log
npm-debug.log*

# OS
.DS_Store

# Local secret helper — never commit (may contain DB password)
server/set-db-password.sh
